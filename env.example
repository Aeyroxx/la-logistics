# L&A Logistic Services - Environment Configuration
# Copy this file to .env.local for local development or .env.production for production

# Application Settings
NODE_ENV=production
PORT=3003
FRONTEND_PORT=3000

# Security (IMPORTANT: Change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-long-and-random

# Database (SQLite file location)
DB_PATH=./backend/database.sqlite

# Upload Settings
UPLOAD_DIR=./backend/uploads
MAX_FILE_SIZE=5242880

# CORS Settings (Add your domain for production)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3004,https://your-domain.com

# Email Settings (Optional - can be configured via admin panel)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=

# Company Settings (Optional - can be configured via admin panel)
COMPANY_NAME=L&A Logistic Services
COMPANY_LOGO=
